<Window x:Class="Anime_Store.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Anime_Store"
        mc:Ignorable="d"
        Title="Anime4every1" Height="564" Width="807.5"
        ResizeMode="CanResizeWithGrip">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid VerticalAlignment="Top" Grid.Row="0">
            <Button Style="{StaticResource ButtonStyle}" Template="{StaticResource ButtonTemplate}" Width="51" Height="51">
                <StackPanel>
                    <Image Source="/Pictures/home_logo.png"/>
                </StackPanel>
            </Button>
            <Button/>
                <Button/>
                <Button/>
                <Button/>
                <Button/>
        </Grid>

        <Grid Grid.Row="1">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="200"/>
            </Grid.ColumnDefinitions>
            <ScrollViewer Grid.Column="0">

            </ScrollViewer>

            <ScrollViewer Grid.Column="1">

            </ScrollViewer>

        </Grid>

    </Grid>

</Window>
